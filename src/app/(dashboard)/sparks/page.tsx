import { SparksManager } from "@/components/sparks-manager";

export const dynamic = "force-dynamic";
export const metadata = { title: "Sparks" };

export default function SparksPage() {
  return <SparksManager />;
}
