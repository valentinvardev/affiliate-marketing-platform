import { RedirectsManager } from "@/components/redirects-manager";

export const dynamic = "force-dynamic";
export const metadata = { title: "Redirecciones" };

export default function RedireccionesPage() {
  return <RedirectsManager />;
}
